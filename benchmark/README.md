# Benchmark

The benchmark harness lands in a later release.

This directory will hold reproducible scripts that compare Budgetary estimates against post-inference ground truth for a set of public prompt corpora.
